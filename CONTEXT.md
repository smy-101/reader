# Reader · AI 阅读器

个人单用户 AI 阅读器:把读的书加入 AI 上下文,边读边讨论。本文件只做一件事:约定"这个词指什么"。需求与决策见 `docs/需求文档.md` 与 `docs/adr/`。

## 书库

**书籍**:
服务端登记的一部 EPUB——元数据、书源文件、章节、划线的整体,以 file_hash 唯一。
_Avoid_: 图书

**书库**:
服务端全部书籍的集合,唯一事实源,端上一律以它为准。
_Avoid_: 书架(那是端上视图)

**书架**:
端上对服务端书库的投影视图;服务端删书后自动移出。
_Avoid_: 书库

**章节**:
EPUB 中**有正文的内容文件**,按阅读顺序平铺;是"整章降级"与切块的单位。
_Avoid_: 目录节点、toc 项(那是导航,不是章节)

**目录**:
EPUB 原文件里的嵌套导航结构,仅由前端渲染,不入库。
_Avoid_: 章节(混用时)

**书源文件**:
服务端落盘的 EPUB 原文件本体。
_Avoid_: 书文件

## 阅读

**划线**:
选中文字产生的高亮(可带颜色/备注),以 CFI 定位,LWW 同步。
_Avoid_: 批注(特指备注字段)、笔记

**阅读进度**:
每本书单行的接续位置(CFI + 百分比),跨端同步。
_Avoid_: 进度条

**阅读设置**:
字号/主题等纯端上偏好,只存本地,不跨端同步(与"阅读进度"严格区分)。

## 端上缓存(安卓)

**本地副本**:
安卓端"打开即下载"形成的完整可读状态;可显式删除,删后重新下载。
_Avoid_: 缓存(太泛)

**文件缓存**:
端上按 file_hash 存的 EPUB 文件副本,多本书去重复用;删除本地副本不必然清文件缓存。
_Avoid_: 本地副本(两者边界见上)

## AI 伴读

**会话**:
围绕一本书(或跨书)的 AI 对话消息序列,可重命名/删除。
_Avoid_: 聊天、对话(指交互行为,不指实体)

**跨书会话**:
不挂书的会话,不随书删除级联。
_Avoid_: 全局会话

**消息**:
会话内一条发言(role + 内容)。
_Avoid_: 聊天记录

**引用**:
消息携带的来源标注(选中文字、章节、划线、向量块等)。
_Avoid_: 附件

**目标章**:
S2 提问所指的章节;由请求显式携带,缺省取当前阅读位置。
_Avoid_: 当前章(易与阅读位置混淆)

**上下文预算**:
发送前装配 prompt 的内容额度:书内容优先占额,剩余装最近消息;不够按 整书 → 目标章 → 检索式 降级。

**四场景(S1–S4)**:
S1 选中即问(只传选中文字)、S2 当前书问答、S3 定位原文(检索 + 跳转)、S4 跨书对比(纯向量多书检索)。

## 向量检索

**向量块**:
章节正文切块后送去 embedding 的文本单位。
_Avoid_: chunk、切片

**嵌入任务**:
对一本书全量生成向量块的后台作业,有状态与进度,失败可重试。
_Avoid_: embedding job
