# 04: web AI 面板 + S2 全链路 E2E

**What to build:** 阅读器里能对着当前书问 AI 并看到流式回复:侧边 AI 面板(该书会话列表、消息流、输入框),提问缺省携带当前阅读位置映射的目标章(显式点名章用显式值,D-31 前端侧),回复逐字流式增量渲染,错误事件显式提示不悬挂,消息的引用来源可见;会话可重命名/删除;刷新/重开该书,会话与消息原样还在。packages/api-client 扩展(新端点类型 + SSE 消费)。E2E harness 内置本地流式 stub LLM(OpenAI 兼容、多块 SSE 响应),E2E 走通:设置页 UI 配置指向 stub → S2 提问 → 流式增量渲染 → 会话与消息落库(resetBackend 直连断言)→ 刷新仍在 → 重命名/删除会话。CI 既有 web job 承载,零新增外网依赖。

**Blocked by:** 03(S2 后端端点与 SSE 契约)

**Status:** ready-for-agent

- [ ] AI 面板:该书会话列表/切换、消息流(含引用展示)、输入提问;回复流式增量渲染,错误/中断显式提示不悬挂
- [ ] 提问缺省携带当前阅读位置所在章;显式点名章用显式值(D-31)
- [ ] 会话重命名/删除生效;刷新/重开会话与消息不丢(FR-304)
- [ ] E2E harness 起本地流式 stub LLM;设置页 UI 配置指向 stub(复用 01 的设置页)
- [ ] E2E 全链路:配置 → S2 提问 → 流式渲染 → 落库直连断言 → 刷新仍在 → 重命名/删除;CI web job 绿,零回归
- [ ] api-client 扩展新端点与 SSE 消费,契约单一出处
