# 02: 预算计算器纯函数 + D-37 计数 ADR

**What to build:** 把 FR-302 的上下文预算策略做成纯函数并用单测钉死(M3 验收标准原文):预算分配 = 书内容优先占额 → 剩余装最近对话消息(装不下丢最旧、不做摘要);书内容塞不下按 整书 → 目标章 → 检索式 降级;单章超限且检索式不可用 → 明确文案"上下文不足,请换大上下文模型或配置 embedding"优雅报错;上下文上限空按 8k 保守计;token 按 D-37 字符近似计数(中文 ≈ 1 token/字、英文 ≈ 1 token/4 字符,向上取整,宁可高估提前降级);S1 的书内容槽 = 选中文字(已与用户确认)。检索式分支以输入标志控制(M3 调用方恒 false,M4 翻真)。token 计数取舍写 ADR(编号顺延)。本票不动任何端点与 UI。

**Blocked by:** None(可立即开始)

**Status:** ready-for-agent

- [ ] 纯函数无 Spring 上下文,普通 JUnit 单测;输出装配计划:模式(整书/目标章/选中文字/检索式/错误)+ 纳入书内容 + 保留消息前缀 + 降级/断尾/不足三类可读文案
- [ ] 边界全覆盖:整书装下 / 整书超限降目标章 / 单章超限且无检索式 → 错误文案 / 选中文字槽(S1)/ 消息断尾丢最旧 / 上限空默认 8000 / 中英混合文本计数 / 恰好等于预算的边界
- [ ] 断尾不做摘要(FR-302 / D-42);检索式分支仅由输入标志控制,M3 不可达
- [ ] D-37 字符近似计数 ADR 落档
- [ ] mvn test 绿,零回归
