# 03: S2 后端 tracer(会话域 + 预算装配 + SSE 流式中继)

**What to build:** S2 当前书问答后端全链路,curl 可演示:模型设置就绪后,向书级提问端点发起提问(content 必填;session_id 可选,缺省=该书最近活跃会话、无则新建、自动标题取首条提问截断;chapter_id/CFI 可选目标章,D-31),后端经预算计算器装配 prompt(书内容优先、剩余装最近对话、降级链),调 OpenAI 兼容 chat completions(stream=true,留 Adapter 口,FR-402),以 SSE 中继返回——开场元数据(落定会话/消息标识)、增量文本、完成、错误四类事件显式下发,上游超时/断流/非 2xx 转错误事件收尾不悬挂(FR-303 / D-25);用户消息受理即落库,助手消息流结束落库(中断时已到内容照常落库)。配套会话列表(最近活跃排序)/重命名/删除/消息拉取(含 refs)端点(FR-301/304);未配置模型设置 → 引导文案。chat_session(book_id 可空外键级联,M4 跨书预留,D-33)与 chat_message 两表经 Flyway 落地,"最近活跃" = updated_at 服务器时钟(新消息/重命名刷新)。全部行为以 Seam A 集成测试落档(本地 OpenAI 兼容 stub 上游,含发给上游的 prompt 形状抽查:目标章内容进 prompt、整书超限时实为目标章)。

**Blocked by:** 01(模型设置域与 LLM 探针基建)、02(预算计算器)

**Status:** done(done(Seam A 18 用例绿:事件序列/路由三态/落库时机/prompt 形状抽查/上游错误形态;mvn test 全绿) 2026-08-29)

- [x] 提问端点返回 text/event-stream:元数据 → 增量… → 完成 正常序列;上游错误/超时/断流 → 错误事件收尾,连接不悬挂(FR-303)
- [x] 会话路由三态正确:指定 session_id / 缺省落该书最近活跃 / 无则新建(标题=首条提问截断)(D-32)
- [x] 用户消息受理即落库;助手消息流结束落库,中断时已到内容照常落库;refs 落库
- [x] 预算装配生效:经 stub 收到的 prompt 断言书内容=整书或降级后的目标章、剩余装最近对话;未配置模型设置 → 引导文案错误
- [x] 会话列表按最近活跃排序、重命名、删除、消息拉取端点齐备且行为正确(FR-304 / FR-301)
- [x] 两表经 Flyway 迁移落地;book_id 可空外键 ON DELETE CASCADE(D-33 预留);updated_at 服务器时钟(D-19 同源)
- [x] LLM 仅 OpenAI 兼容协议并留 Adapter 接口(FR-402);新端点 401;错误文案可读
- [x] Seam A(stub 上游)覆盖上述全部,mvn test 绿,零回归
