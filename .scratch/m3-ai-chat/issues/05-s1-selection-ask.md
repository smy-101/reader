# 05: S1 选中即问(划选 → 问 AI → 流式返回)

**What to build:** 划一段文字直接问(M3 验收第一条原文):阅读器里划选后菜单出现"问 AI"入口(复用 M1 已验证的拖选手势链路),带出提问框;提问携带 selection(选中文字 + CFI),预算的书内容槽 = 选中文字(不装整书/整章,S1 口径已确认),剩余额度照常装最近对话;与 S2 走同一提问端点同一消息通路(D-32),自动落该书最近活跃会话(无则新建),消息流中该条提问可见选中文字引用。Seam A 补 selection 通路集成断言;E2E 用真实拖选手势:划选 → 问 AI → 流式增量返回 → 落库断言。

**Blocked by:** 04(AI 面板与流式渲染、E2E stub 基建)

**Status:** ready-for-agent

- [ ] 划选菜单"问 AI"入口;提问携带 selection(文字 + CFI);后端落 refs、预算书内容槽 = 选中文
- [ ] S1 与 S2 同端点同通路(D-32);自动落该书最近活跃会话,无则新建
- [ ] 消息流中该条提问可见选中文字引用(引用可回溯,FR-301)
- [ ] Seam A 补 selection 通路的集成断言(prompt 含选中文、refs 落库)
- [ ] E2E:真实拖选手势划选 → 问 AI → 流式增量返回 → 落库直连断言(M3 验收"划选提问流式返回"原文);零回归
