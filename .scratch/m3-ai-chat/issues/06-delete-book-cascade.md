# 06: 删除书籍 + 完整级联(FR-104 兑现)

**What to build:** 书库管理闭环(FR-104,自 M1 推迟至此兑现):web 书库删书入口 + 确认弹窗明示级联范围(书源文件/封面、划线、进度、该书全部会话);后端 DELETE 书籍端点执行完整级联——磁盘书源文件与封面清理、划线/进度/该书会话经外键级联全清;book_id 为空的跨书会话不受影响(D-33);向量级联为 M4 在同一删除流程上的增量(流程单点挂接,不散落)。Seam A 断言级联彻底(文件 + 三域数据无残留、跨书会话不动),E2E 断言弹窗与删后书库/会话入口状态。

**Blocked by:** 03(chat_session 表存在,会话级联才可断言)

**Status:** ready-for-agent

- [ ] 删除端点:书源文件与封面从磁盘清除,划线/进度/该书会话全清,DB 无残留
- [ ] book_id 为空的会话不受级联影响(D-33)
- [ ] 前端确认弹窗明示级联范围;删除后书库列表即时移出(FR-104)
- [ ] 删书流程为 M4 向量清理预留单一挂点
- [ ] Seam A 级联断言全绿(含 401);E2E 弹窗 → 删除 → 书库与该书会话入口消失;零回归
