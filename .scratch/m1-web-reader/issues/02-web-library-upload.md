# 02: apps/web 起步:书库列表 + 过滤 + 上传(单/批量)

**What to build:** 第一个前端切片:纯浏览器完成藏书导入与浏览。React + TypeScript 应用(apps/web)落位;packages/api-client 起步——共享 TS 类型 + API client,Bearer token 经配置注入,先覆盖上传/书库列表/封面三个既有端点。书库列表页显示封面、标题、作者(进度百分比暂为占位,06 接通);按标题/作者即时过滤(纯前端,FR-106);单本上传成功即入列表;多选批量上传前端循环调单文件接口、逐本展示结果(D-43),重复书按幂等返回提示"已在书库"并继续下一本(D-30);损坏/DRM/超限等后端可读错误文案直接透出。

**Blocked by:** None(可立即开始;与 01 spike 互不阻塞)

**Status:** ready-for-agent

- [ ] apps/web(React+TS)应用可本地启动,经 api-client 带 token 调通后端,token 可配置
- [ ] 书库列表:封面、标题、作者;按标题/作者即时过滤(FR-103/106)
- [ ] 单本上传成功后立即出现在书库列表
- [ ] 多选批量上传:循环逐本调单文件接口,逐本展示结果,重复/失败不打断整批(D-43)
- [ ] 重复上传提示"已在书库";损坏/DRM/超限错误以可读文案透出(D-30 / M0 文案)
- [ ] packages/api-client 类型与 client 就位、token 注入,后续票可在此扩展
