# 05: 阅读器主链路:渲染 + 目录导航 + 本地设置

**What to build:** 浏览器里真正读起书来。从书库列表点开一本书进入阅读页:渲染引擎按 01 spike 定案落地(默认 foliate-js vendor 源码);书源文件经 api-client 带 token 拉取(不泄漏 token 到 URL);正文渲染并可翻页/滚动阅读;目录面板由前端从 EPUB 原文件解析嵌套结构渲染(不入库,D-40),点击条目跳转对应位置;字号/主题基本设置可调,仅存 localStorage、不进任何后端接口(FR-201)。E2E(挂在 03 harness 上):书库→打开→正文渲染→目录跳转→调字号/主题→刷新页面后设置保留。

**Blocked by:** 01(引擎定案), 02(书库列表与 api-client), 04(书源文件下载)

**Status:** done

- [x] 从书库列表点开书,阅读页渲染正文,可翻页/滚动阅读
- [x] 目录面板从 EPUB 原文件渲染嵌套结构,点击条目跳转到对应位置(D-40,不走后端)
- [x] 字号/主题可调,仅存 localStorage,刷新后保留;阅读全程不调用后端保存设置(FR-201)
- [x] 书源文件经 api-client 带 token 拉取
- [x] E2E 用例:打开→渲染→目录跳转→刷新后设置仍在,CI apps/web job 保持绿
