# 07: 阅读器划线:选中→高亮/颜色/备注 + 全量渲染

**What to build:** 选中即划线落在真实阅读器里(07 = 划线 UX 端到端)。在阅读页选中一段正文:取出 CFI 与文字快照创建划线,高亮即时渲染;创建时可选颜色、可加备注;点击已有高亮可改颜色/改备注/删除,变更即时同步服务端;打开书时一次全量拉取该书划线并全部渲染(D-24);提供书内划线列表,可浏览与管理该书全部划线。E2E(挂在 03 harness 上):选中→高亮可见→改色/改备注→刷新→全部仍在。选中手势的自动化策略按 01 spike 结论执行:可驱动则全自动;不可行则 E2E 以程序化创建划线验证渲染与同步,选中手势进人工验收清单,不阻塞。

**Blocked by:** 03(E2E harness), 05(阅读页), 06(划线 API)

**Status:** ready-for-agent

- [ ] 选中正文可创建划线:高亮即时渲染,CFI 与文字快照入库(FR-202)
- [ ] 划线可选颜色、可加备注;点击高亮可改颜色/备注、可删除,变更即时同步服务端
- [ ] 打开书一次全量拉取划线并全部渲染(D-24);刷新/重开后仍在
- [ ] 书内划线列表可浏览全部划线并管理
- [ ] E2E:创建→改→刷新→仍在,全绿(手势策略按 01 结论);CI apps/web job 保持绿
