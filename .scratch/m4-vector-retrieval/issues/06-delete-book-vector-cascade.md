# 06: 删书向量级联钉死(FR-104 最后一块)

**What to build:** 删除书籍时**向量块与嵌入任务一并清净**。外键级联随嵌入任务域的 Flyway 迁移落地后,本票把行为钉死进 Seam A 用例(含嵌入进行中删书的竞态面:任务随之清除不留尸块、不阻塞删除),并把前端删书确认弹窗的级联范围文案补上"向量块/嵌入任务"。既有删书级联回归不变。

**Blocked by:** 02(两表与嵌入任务落地)

**Status:** ready-for-agent

- [ ] Seam A:删除已嵌入的书 → 向量块与嵌入任务行全清,既有级联(文件/划线/进度/该书会话、跨书会话不动)回归不变
- [ ] 嵌入进行中删书:任务与已入块清净,删除不被阻塞、无残留
- [ ] 前端确认弹窗级联范围文案含向量块/嵌入任务
- [ ] 既有删书 E2E 回归绿
