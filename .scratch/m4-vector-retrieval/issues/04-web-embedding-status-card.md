# 04: web 嵌入状态卡(状态/进度/重试/重嵌入入口)

**What to build:** 书库/书详情可见每本书的**嵌入状态卡**:进行中显示进度(已完成块数/总块数),失败显示可读错误与"重试"入口,完成显示所用模型;提供"重新嵌入"入口(存量书首次嵌入与换模型全量重嵌入同一入口,多态一语义)。**未配置 embedding 时状态卡与全部相关入口隐藏**,优雅降级不报错(FR-403)。E2E:设置页配 stub embedding → 上传书 → 状态卡进度推进至完成。桌面壳零改动继承(壳内即 Web)。

**Blocked by:** 02(嵌入任务域:状态/触发端点与 api-client 方法)

**Status:** ready-for-agent

- [x] 已配置 embedding:嵌入状态卡可见,进行中进度推进,done/failed 状态与错误文案正确
- [x] 失败 → "重试"入口触发后任务重跑至终态;"重新嵌入"入口可对存量书/已完成书触发
- [x] 未配置 embedding:状态卡与相关入口全部隐藏,不报错(FR-403)
- [x] E2E(stub embeddings):配置 → 上传 → 进度至完成全链路绿
- [x] 桌面壳零改动继承
