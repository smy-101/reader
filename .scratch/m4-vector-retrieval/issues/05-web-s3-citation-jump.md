# 05: web S3 定位原文(入口 + 引用条 + 章内摘录定位)

**What to build:** AI 面板新增"定位原文"提问入口(**该书嵌入完成才显示**,未配置 embedding/未嵌入完成时隐藏不报错,FR-403):提问后流式回复照常渲染,引用条随开场元数据事件在流式开始前即可见(章节标题 + 原文摘录);**点击引用跳转到阅读器对应章节**,并尝试以摘录文字在章内定位——命中滚动到命中处,未命中停在章首不报错(D-40 清洗口径,不承诺 CFI 级精确定位)。E2E stub 全链路:配置 embedding → 上传嵌入完成 → "定位原文"提问 → 流式回复 + 引用条 → 点击跳转章节断言 → 刷新后引用仍在。桌面壳零改动继承。

**Blocked by:** 03(检索式提问端点与引用下发;嵌入状态判断走 02 的 api-client 方法,不硬依赖 04)

**Status:** ready-for-agent

- [ ] "定位原文"入口仅该书嵌入完成时显示;未配置/未嵌入完成时隐藏不报错(FR-403)
- [ ] 流式回复渲染照常(SSE 增量),引用条在流式开始前即可见
- [ ] 点击引用跳转到对应章节;摘录文字章内命中则定位滚动,未命中停章首不报错
- [ ] 刷新/重开会话后引用条仍在(refs 持久化)
- [ ] E2E(stub embeddings + chat)全链路绿;桌面壳零改动
