# 04: 书库查询 API:列表 / 详情 / 章节列表 / 封面服务

**What to build:** 上传之后的读侧:带 token 查书库列表(封面 URL、标题、作者;阅读进度百分比字段占位恒空,待 M1 接通)、书籍详情、章节列表(按 seq 有序)、通过 URL 直接拉取封面图片。完成后可演示:上传的书出现在书库列表,封面能显示,章节结构可查。

**Blocked by:** 03(EPUB 上传全链路)

**Status:** done (2026-08-28)

- [x] 书库列表返回已上传书籍:封面 URL、标题、作者(FR-103);进度百分比字段占位恒空
- [x] 书籍详情与章节列表(按 seq 有序)可查,与上传时的入库内容一致
- [x] 封面 URL 可直接取得图片文件,与其他接口同样受 token 保护
- [x] 所有接口无/错 token 一律 401,行为与 01 一致
- [x] 集成测试覆盖读路径:上传后列表可见、详情正确、章节有序、封面可取
