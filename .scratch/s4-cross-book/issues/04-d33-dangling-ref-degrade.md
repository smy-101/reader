# 04: D-33 悬空引用降级(删书留会话 + 占位渲染)

**What to build:** 兑现 D-33 的两半。后端侧钉死:删除一本书 → 跨书会话与消息**原样保留**(book_id 为空,FK 级联天然不触及)、refs 书名快照不动——用集成测试把这笔"天然行为"变成有守卫的契约。web 侧:重开跨书会话时,被删书籍的历史引用降级渲染为「《书名快照》(原书已删除)」占位且不可点击(判定 = 引用里的书不在书库列表中);未删书籍的引用照常渲染、照常可跳。前端删书确认弹窗文案不需为此变化(跨书会话本就不在级联范围,与 FR-104 明示的级联范围一致)。

**Blocked by:** 02(全局面板与引用条;refs 书字段来自 01)

**Status:** ready-for-agent

- [ ] Seam A:删一本书 → 跨书会话与消息原样保留、refs 快照不动(FK 天然行为钉死为契约)
- [ ] web:被删书籍的历史引用显示「《书名》(原书已删除)」占位,不可点击跳转
- [ ] 未删书籍的引用照常渲染、可跳(03 已交付时)
- [ ] E2E:删书 → 重开跨书会话 → 占位断言;删书级联(书级会话清、跨书会话留)双面回归
