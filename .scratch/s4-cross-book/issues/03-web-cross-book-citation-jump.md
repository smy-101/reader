# 03: web 跨书引用跳转(打开另一本书 + 章内定位)

**What to build:** 跨书对比结论一步回原文:在全局 AI 面板点击指向"当前没在读的那本书"的引用 → 打开对应书籍进入阅读器 → 跳转到引用章节,并尝试以摘录文字在章内定位——命中滚动到命中处,未命中停在章首不报错(复用 S3 定位口径,D-40 清洗口径不承诺 CFI 级精确)。跳转信号经应用顶层状态传递(书库视图 → 打开阅读器 → 就绪后执行定位)。指向当前已打开书籍的引用与书级 S3 跳转行为保持一致。

**Blocked by:** 02(全局面板与引用条)

**Status:** ready-for-agent

- [ ] 点击另一本书的引用 → 打开该书阅读器并跳到对应章节
- [ ] 章内摘录命中滚动定位;未命中停章首不报错
- [ ] 指向当前书的引用跳转与书级 S3 行为一致
- [ ] E2E:两本书场景,点击跨书引用断言打开另一本书并定位;书级 S3 跳转回归绿
