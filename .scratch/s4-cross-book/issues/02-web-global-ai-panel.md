# 02: web 全局 AI 面板(书库页入口 + 跨书会话 + 引用条)

**What to build:** 浏览器可演示的 S4 主入口:书库页出现「AI 伴读」全局入口——未配置 embedding 或全库没有任何就绪书时隐藏(FR-403;就绪摘要来自书库列表响应的扩展,与嵌入状态卡同源消费,不逐书轮询拼判断);点击打开跨书会话面板,与阅读器内 AI 面板同构:会话列表(最近活跃)+ 消息流 + 输入框 + 重命名/删除。跨书提问走全局端点,流式增量渲染,引用条(书名 + 章节标题 + 原文摘录)随 meta 事件在流式开始前即可见;错误显式提示不悬挂;刷新/重开面板会话、消息与引用仍在。实现含一处预重构:从既有 AI 面板抽出可复用的消息流/引用条/输入部分供两处共用,书级面板行为零变化(经既有 E2E 回归保证)。桌面壳零改动继承。

**Blocked by:** 01(全局提问/列表端点与 api-client 类型)

**Status**: done

- [x] 书库页「AI 伴读」入口:未配置 embedding / 全库无就绪书时隐藏;配置且有就绪书时出现
- [x] 跨书会话面板四件事齐全:列表(最近活跃)/ 消息流 / 提问输入 / 重命名删除
- [x] 跨书提问流式渲染;引用条(书名 + 章节 + 摘录)在流式开始前可见
- [x] 刷新/重开面板,会话、消息与引用仍在(refs 持久化)
- [x] 提问中途出错显式提示,不永久转圈(FR-303)
- [x] 书级 AI 面板回归零变化;跨书会话不出现在任何书级会话列表
- [x] AI 面板共用部分抽取后,书级 S1/S2/S3 既有 E2E 回归绿
- [x] E2E(stub embeddings + chat)全链路绿:入口出现 → 跨书提问 → 流式 + 两书引用条 → 刷新仍在
